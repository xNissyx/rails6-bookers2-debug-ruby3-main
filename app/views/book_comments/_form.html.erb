<%= form_with model: BookComment.new, url: book_book_comments_path(book.id), local: false do |f| %>
  <%= f.text_area :body, class: "w-100", style: "height: 8rem;"  %><br>
  <%= f.submit "送信" %>
<% end %>